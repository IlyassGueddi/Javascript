// *----- Arrays -----* //

let chaos = ["ilyass", "Manal" , "mohammed", 12, true]
let length = chaos.length


// this will print all the elements of the array
for (i = 0; i < length; i++) {
    console.log(chaos[i])
}